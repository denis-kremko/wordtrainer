<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
	<!-- Custom keys must live in a real Info.plist: Xcode's INFOPLIST_KEY_* build
	     settings only work for known system keys and silently drop custom ones.
	     This file is merged with the generated Info.plist (GENERATE_INFOPLIST_FILE). -->
	<!-- Opt out of iOS 26 Liquid Glass: the floating glass tab bar ignores
	     toolbarBackground, and the design language is all translucency —
	     the opposite of this app's flat opaque theme. -->
	<key>UIDesignRequiresCompatibility</key>
	<true/>
	<key>DictionaryDownloadURL</key>
	<string>https://github.com/denis-kremko/wordtrainer/releases/download/dict-v13/dictionary-v13.sqlite.gz</string>
	<!-- SHA256 of the *decompressed* dictionary.sqlite. Update together with the URL
	     when publishing a new dictionary release. -->
	<key>DictionarySHA256</key>
	<string>654fd3d44b988de4d655084c9a6283ace4074eb7f4e346e466d1681df67e8980</string>
</dict>
</plist>
